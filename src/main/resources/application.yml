spring:
  application:
    name: Stack Exchange

---
spring:
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true

logging:
  level:
    rip.opencasket: DEBUG
    org.hibernate: INFO